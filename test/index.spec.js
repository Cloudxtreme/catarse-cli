var builder = ('../src/builder');

describe('CLI tool', function(){
	it('should prompt a given question and return its answer', function(){
    pending('must find the best way to test stdout ouput.');
  });
  describe('list of actions', function(){
    it('should set a file name and write a file with it', function(){
      pending('must find the best way to test file writing.')
    })
  })
});